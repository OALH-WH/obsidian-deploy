---
{"dg-publish":true,"permalink":"/技术文档/Linux/Linux启动引导/uboot/常用命令/","dg-note-properties":{}}
---

# 引导内核相关
## `boot`
- 默认执行`bootcmd`中指定的命令
## 网络相关
# `ping <ip>`
- 向服务端发送ICMP报文, 测试网络连通性
---
# 环境变量配置
## `printenv`
- 打印所有环境变量以及值
# `saveenv <environment variable> <value>
- 保存所有环境变量的设置
## `print <environment variable>`
- 打印某一个环境变量以及值
---

4. 格式

5. nfs <loadAddress> <serverIP>:<serverPath>

6. ping

7. env

8. print <environment>: 打印环境变量的值

9. save:作用同saveenv

10. saveenv:保存env的配置

11. boot:开始引导系统的启动(从bootcmd默认加载)

12. bootm:指定内存启动内核

13. bootcmd: shell脚本

14. ext4ls: 从<dev>上的<interface>里的<directory>列出文件

15. dev: 设备号

16. interface: 接口类型,例如mmc,usb等,可以通过看是否支持mmc,usb等命令来判断是否支持此接口

17. directory: 目录路径(可选)

18. eg. ext4ls mmc 0:1 [directory] //ext4ls <interface> <dev[:part]>

19. ext4load:从ext4文件系统中加载文件到内存

20. mmc

21. list:可以列出设备号

22. part

23. list

24. part list <interface> <dev>:查看指定设备的分区

25. 环境变量的意义以及格式

26. 