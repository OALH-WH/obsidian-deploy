---
{"dg-publish":true,"permalink":"/技术文档/C_C++/C++/第三方库/Qt/QT/QML/旧的/常用组件/","dg-note-properties":{}}
---

1. 规范

1. 命名：QML文件名首字母必须大写

1. 语法

1. 组件定义：组件 {}

1. 事件定义：事件：{} or 事件：函数调用

1. 属性定义：属性：值

1. 自定义属性：property 属性类型 自定义属性 = 值

1. 自定义函数：function 函数名（参数）{函数体}

1. 赋值用=

1. 初始化用:

1. if...else后面跟{}

1. on<property>Changed函数：可以用来当属性值被修改时自定义操作

1. 示例：on<property>Changed: {}

1. 基础模块

1. QtQuick 2.0

1. QtQuick.Controls

1. 引入了Button组件

1. 基本组件之间的关系

1. Item：所有可视元素的基类（无法直接显示，需要在Window or QuickView中才能显示）

1. Window：顶级窗口组件

1. ApplicationWindow

1. 基本组件

1. Application：一般作为app的根组件

1. ChildWindow

1. Text组件：用来显示文本，只读，不支持用户输入

1. 属性

1. text：文本内容

1. 子组件

1. TextField组件：文本框，支持读写

1. 属性

1. echoMode：TextInput类型//文本显示模式

1. TextInput.Password

1. TextInput.PasswordEchoOnEdit

1. TextInput.Normal

1. MouseArea：为了让没有点击事件的组件而设计的

1. 事件

1. onClicked：点击区域触发

1. onDoubleClicked：双击区域触发

1. onPressed：点击按下区域触发

1. onRelease：点击松开区域触发

1. onPressAndHold：长按区域触发

1. onEntered：点击区域或者点击离开区域再进入区域触发

1. onExited：点击区域松开或离开区域触发

1. Button

1. background：Rectangle组件

1. 事件

1. onClicked

1. Rectangle：相当于Widget，继承自Item

1. 相较于Item多的属性

1. antialiasing：bool//抗锯齿，默认是true

1. border.color

1. border.width

1. color

1. radius: 10//四角的圆角半径

1. z：调整层数，层数越大，显示优先级越高

1. rotation：旋转度数

1. scale：缩放倍数

1. anchors(锚点定位)属性示例

1. anchors.fill: parent

1. 作用：填充满父组件

1. anchors.left: <Id>.right

1. 作用：把当前组件放在id为xxx组件的右边

1. anchors.leftMargin: 20

1. 作用：相对于当前组件，左边+20

1. anchors.centerIn: parent

1. 作用：相对于父组件的中间位置

1. Window

1. 基本属性

1. width：窗口宽度

1. height：窗口高度

1. visible：窗口是否显示

1. title：窗口标题

1. x/y:窗口位置

1. minimumWidth/Height

1. maximumWidth/Height

1. opacity：透明度

1. 常用方法

1. console.log(); : 打印日志到输出终端

1. 动画

1. 普通动画

1. ProopertyAnimation：可以创建一个属性的动画

1. 属性（*为必须设置的属性）

1. target*: 组件ID，可以绑定多个，格式为：[组件1，组件2]

1. properties*：将动画运用到哪个属性上

1. from：动画开始状态

1. to*：动画结束状态

1. duration：动画持续时间，默认250，即0.25秒

1. easing:设计动画曲线，例如easing{}

1. 函数

1. id.start():开启动画

1. 自动触发动画

1. 在组件定义后面加on

1. 示例

1. PropertyAnimation on x:表示绑定的组件的x属性从运行开始自动变化

1. 多播动画

1. ParallelAnimation模块

1. 队列动画

1. 介绍：即存储了动画组件的队列，也可以自动触发

1. SequentialAnimation组件

1. 函数

1. start():开启动画

1. 状态动画和状态过渡

1. transitions：是一个状态过渡列表

1. Transition组件，用来设置从一个状态到另一个状态的过渡动画

1. 属性

1. from：设置从什么状态

1. to：设置到什么状态

1. 动画组件，例如PropertyAnimation，ColorAnimation组件

1. 

1. states：是一个状态列表，例如states: [ State: {} State: {}]

1. State组件,例如State: {name: str PropertyChanges: {}}

1. 属性

1. name: 字符串，后面使用状态动画的组件通过设置自身state属性为对应的State组件name属性的值来指定使用哪个状态

1. PropertyChanges组件 

1. 属性

1. target

1. ...

1. 行为动画，即设置一个：只要一个属性改变，就会有的动画

1. Behavior组件，例如Behavior on 属性{}

1. 使用行为动画的方式

1. 直接在组件里面定义

1. Behavior组件里定义（引用别的地方已经定义的行为动画）

1. animation：动画组件id

1. Component:对于用Component包裹的组件，他不会立即将该组件生成到窗口，而是需要Loader组件的配合

1. 事件

1. onCompleted：当组件完成创建，并且子组件的创建和初始化也完成时触发

1. Loader：用来生成组件到窗口

1. 属性

1. asynchronous：是否异步加载，值为bool

1. true：加载组件到窗口

1. false：不加载

1. sourceComponent：Component组件ID

1. source: QML文件名 //一个QML文件可以作为一个组件被loader动态加载

1. 图片组件

1. Image

1. 属性

1. source： 字符串，图片路径

1. //可通过width和height设置图片宽高，但是会变形

1. 动图加载

1. AnimatedImage

1. 属性

1. source

1. playing：bool//是否播放

1. paused：bool//是否停止

1. speed：int//动图加速