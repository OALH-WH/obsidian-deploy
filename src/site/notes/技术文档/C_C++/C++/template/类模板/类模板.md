---
{"dg-publish":true,"permalink":"/技术文档/C_C++/C++/template/类模板/类模板/","dg-note-properties":{}}
---

1. 注意

1. 静态成员变量定义/初始化

1. 版本

1. before

1. 类外定义 //通常放在头文件和模板类的声明定义一起

1. 不能放在源文件中，会导致包含了该类模板头文件的编译单元中找不到模板类成员变量的定义，无法实例化，进而报错未定义的引用

1. 遵循静态变量只能有一个定义

1. 不会发生冲突，因为类模板只是一个蓝图，实际实例化在链接阶段，会为不同的模板参数生成不同的定义

1. 示例

1. eg. template <typename 模板参数> MyClass<模板参数>::成员变量;

1. c++17

1. 和普通类的区别

1. 类名不是类型，类型是eg. my_class<T> //普通类的类名即是类型

1. 不支持声明写到.h，定义写到.cpp这种声明和定义分开的方式，会报链接错误

1. 原因：

1. 声明和定义

1. 声明

1. eg. template<class T, class X>  struct A{int testFunc();};

1. 定义 //比声明多了一个类型名（不是类名）

1. eg. template<class T, class X> A<T, X>::testFunc(){} 