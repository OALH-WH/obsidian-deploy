---
{"dg-publish":true,"permalink":"/技术文档/C_C++/C++/标准库/libstdc++/智能指针/","dg-note-properties":{}}
---

1. 支持版本	

1. c++98 提供了auto_ptr //11版本已弃用，17版本被正式移除

1. c++11 提供了unique_ptr, shared_ptr, weak_ptr

1. 头文件：memory

1. 常用函数

1. get：获取内存首地址

1. return：内存首地址

1. release：取消对内存地址的托管

1. return：内存首地址

1. reset：重置内存的管理，析构内存，并根据参数重新托管内存，不指定参数则设置为NULL

1. 分类

1. auto_ptr

1. 注意

1. 不支持直接的数组内存分配，析构内存时用的是delete，而不是delete[]，因此会导致使用了数组内存时内存泄漏

1. unique_ptr

1. 注意

1. 不支持拷贝构造和拷贝赋值

1. shared_ptr

1. 原理

1. 计数

1. 盲猜是通过模板类的类变量（静态变量）实现

1. 计数增加的情况

1. 复制构造

1. operation=

1. 使用shared_from_this()

1. 移动语义后的赋值，移动语义不会增加赋值

1. 在容器中存储

1. 注意

1. 支持多个指针共享一块内存区域

1. 多一个指针访问，access_count + 1, 只有当access_count 为零时才会释放内存git

1. 初始化方法

1. make_shared<类名>（参数列表）

1. 方法

1. use_count() //获取引用计数的值

1. swap() //交换托管的对象，引用计数不变

1. weak_ptr

1. 注意