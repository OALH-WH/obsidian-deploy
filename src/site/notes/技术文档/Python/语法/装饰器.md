---
{"dg-publish":true,"permalink":"/技术文档/Python/语法/装饰器/","dg-note-properties":{}}
---

# 背景
```python
@decorator_a
@decorator_b
@decorator_c
def my_func():
    pass

# 等价于
my_func = decorator_a(decorator_b(decorator_c(my_func)))
```
# 机制
- **函数指针/函数对象 + 闭包 + 运行时元编程** 的组合机制
## 以背景中的示例举例执行步骤
1. 先执行
# 原理
# 定义
## 带参
## 不带参
```python
def wrapper():

```
# 调用