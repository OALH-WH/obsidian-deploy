---
{"dg-publish":true,"permalink":"/技术文档/Python/语法/类/","dg-note-properties":{}}
---

1. 常用方法

1. super函数

1. 参数

1. arg1: 当前类

1. arg2: self

1. 作用

1. 用来显示调用父类方法

1. __str__函数

1. 作用：当用类似于print方法打印类的时候，被调用

1. 示例

```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def __str__(self):
        return f"Person(name={self.name}, age={self.age})"
    
    def __repr__(self):
        return f"Person('{self.name}', {self.age})"
    
# 创建一个 Person 对象
p = Person("Alice", 30)

# 打印对象（调用 __str__）
print(str(p))  # 输出: Person(name=Alice, age=30)

# 直接在解释器中输入对象（调用 __repr__）
print(repr(p))  # 输出: Person('Alice', 30)
```

1. __enter__函数

1. 作用：通常于with语句一起使用，它定义了进入一个上下文时应该执行的操作。简单来说是在with块开始时被调用

1. 例子

```python
class MyContext:
    def __enter__(self):
        print("Entering the context")
        return self

    def __exit__(self, exc_type, exc_value, traceback):
        print("Exiting the context")

with MyContext() as context:
    print("Inside the context")

```