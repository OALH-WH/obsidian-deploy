---
{"dg-publish":true,"permalink":"/技术文档/公司/sino/SONIC/调试手段/","dg-note-properties":{}}
---

1. sith

1. 开sie的光

1. sith product config sie-cpld 0x68  0

1. 调试solink接口

1. 示例

1. link_udp_static -p /board_info 172.16.203.2:6200

1. 常用命令

```cpp
redis-cli -n 4 keys TELEMETRY* |xargs echo| sed -E -n 's/([a-zA-Z|_]*)\s?/"\1" /gp'

rm -fr /sonic/src/sonic-mgmt-framework/vendor/github.com/Azure/sonic-mgmt-common/translib &&\
sudo cp -al /sonic/src/sonic-mgmt-common/translib /sonic/src/sonic-mgmt-framework/vendor/github.com/Azure/sonic-mgmt-common/translib &&\
source build_env ; make ; scp build/rest_server/rest_server root@192.168.70.99:/etc/sonic/wuhang/


rm -fr /sonic/src/sonic-telemetry/vendor/github.com/Azure/sonic-mgmt-common/translib &&\
sudo cp -al /sonic/src/sonic-mgmt-common/translib /sonic/src/sonic-telemetry/vendor/github.com/Azure/sonic-mgmt-common/translib &&\
source build_env ; make ; scp build/bin/dialout_* root@192.168.70.99:/etc/sonic/wuhang/

source build_env ; make rest-server ; scp build/rest_server/rest_server root@192.168.70.100:/etc/sonic/wuhang/


cmake --build obj-aarch64-linux-gnu； scp obj-aarch64-linux-gnu/src/constant_power root@192.168.70.99:/etc/sonic/wuhang

docker cp dialout_server_cli telemetry:/ ; docker cp dialout_client_cli telemetry:/; docker cp rest_server mgmt-framework:/

ps aux | grep dialout_client_cli | awk '!/grep/{print $2}' |xargs kill -9 

docker cp dialout_server_cli telemetry:/ ; docker cp dialout_client_cli telemetry:/; docker cp rest_server mgmt-framework:/; ps aux | grep dialout_client_cli | awk '!/grep/{print $2}' |xargs kill -9

(wuhang test) (.*?,)
$1 sc.name=%v $2

source build_env ; make ; scp build/bin/dialout_* root@192.168.21.34:~/

source /etc/sonic/wuhang/run_env

for i in $(redis-cli -p 5002 -n 5 scan 0 MATCH *FLEX_COUNTER_TABLE:1S_STAT_STATUS* COUNT 1000);do if [ $i == '0' ];then continue; fi ;echo $i  redis-cli -p 5002 -n 5 field=$(hscan "$i"  0 MATCH *LINECARD* COUNT 1000); if [  ];then  ;fi;done

for i in $(git status | grep modified | awk  '{print $2}');do \
rm /sonic/src/sonic-telemetry/vendor/github.com/Azure/sonic-mgmt-common/$i ; \
ln /sonic/src/sonic-mgmt-common/$i /sonic/src/sonic-telemetry/vendor/github.com/Azure/sonic-mgmt-common/$i; \
done

curl -u admin:sino-telecom -X PUT 'http://127.0.0.1:8080/restconf/data/openconfig-wavelength-router:wavelength-router/media-channels/channel' \
-H 'Content-Type:application/yang-data+json' \
-D '{"channel":[{"config":{"attenuation-control-mode":"ATTENUATION_FIXED_LOSS","index":1,"lower-frequency":193550000,"upper-frequency":193650000},"spectrum-power-profile":{"distribution":[{"lower-frequency":193550000,"upper-frequency":193650000,"config":{"attenuation-value":2}}]},"index":1,"dest":{"config":{"port-name":"PORT-1-5-COMOUT"}},"source":{"config":{"port-name":"PORT-1-5-MD1IN"}}},{"config":{"attenuation-control-mode":"ATTENUATION_FIXED_LOSS","index":2,"lower-frequency":193550000,"upper-frequency":193650000},"spectrum-power-profile":{"distribution":[{"lower-frequency":193550000,"upper-frequency":193650000,"config":{"attenuation-value":2}}]},"index":2,"dest":{"config":{"port-name":"PORT-1-5-MD1OUT"}},"source":{"config":{"port-name":"PORT-1-5-COMIN"}}}]}' &&\
echo ""



```