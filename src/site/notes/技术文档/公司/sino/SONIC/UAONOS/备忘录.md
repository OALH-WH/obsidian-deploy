---
{"dg-publish":true,"permalink":"/技术文档/公司/sino/SONIC/UAONOS/备忘录/","dg-note-properties":{}}
---

1. p350c flexcounter

```json
,
    {
        "PORT_COUNTER_ID_LIST_STATUS": {
{% for id in port_attr_id_list %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "PCH_COUNTER_ID_LIST_STATUS": {
{% for id in pch_attr_id_list %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "LCH_COUNTER_ID_LIST_STATUS": {
{% for id in lch_attr_id_list %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "ETHERNET_COUNTER_ID_LIST_STATUS": {
{% for id in ethernet_attr_id_list %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "LLDP_COUNTER_ID_LIST_STATUS": {
{% for id in lldp_attr_id_list %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "OTN_COUNTER_ID_LIST_STATUS": {
{% for id in otn_attr_id_list %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "ASSIGNMENT_COUNTER_ID_LIST_STATUS": {
{% for id in assignment_attr_id_list %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "LINECARD_COUNTER_ID_LIST_GAUGE": {
{% for id in linecard_gauge_id_list  %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "PORT_COUNTER_ID_LIST_GAUGE": {
{% for id in port_gauge_id_list  %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "PCH_COUNTER_ID_LIST_GAUGE": {
{% for id in pch_gauge_id_list  %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "OTN_COUNTER_ID_LIST_GAUGE": {
{% for id in otn_gauge_id_list  %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "OCH_COUNTER_ID_LIST_GAUGE": {
{% for id in och_gauge_id_list  %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "INTERFACE_COUNTER_ID_LIST_COUNTER": {
{% for id in interface_stat_id_list  %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "OTN_COUNTER_ID_LIST_COUNTER": {
{% for id in otn_stat_id_list  %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    },
    {
        "ETHERNET_COUNTER_ID_LIST_COUNTER": {
{% for id in ethernet_stat_id_list %}
            "attr{{loop.index}}": "{{ id }}"{% if not loop.last %},{% endif %}

{% endfor %}
        },
        "OP": "SET"
    }
```