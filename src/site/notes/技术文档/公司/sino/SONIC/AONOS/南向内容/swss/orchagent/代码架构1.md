---
{"dg-publish":true,"permalink":"/技术文档/公司/sino/SONIC/AONOS/南向内容/swss/orchagent/代码架构1/","dg-note-properties":{}}
---

1. ochorch.cpp

1. 构建函数

1. 调laiobjectorch.cpp的构建函数

1. 调orch.cpp的构建函数

1. laiobjectorch.cpp

1. dotask

1. 遍历需要同步的table attr变动

1. 判断attr的变动方式，eg. set 或 del

1. 检测到config done的时候，create object

1. linecardorch.cpp

1. dotask

1. doAppLinecardTableTask

1. 如果状态为state ready，就会尝试创建linecard

1. 会更改状态从ready到work

1. portorch.cpp

1. m_createFunc

1. 最终实现调用的是lairedis的Lai.cpp的Lai::create

1. orch.cpp

1. 构建函数

1. 根据参数connectors对应的table names添加到consumer中

1. dotask

1. 不用管，会走laiobjectorch的dotask

1. 遍历这个consumer里的改动

1. set

1. addOperation

1. del

1. delOperation

1. 以ocm set scan举例

1. doTask: ocmorch.cpp

1. 用lai ocm api中的set函数设置属性到lai接口

1. lai ocm api定义在laihelper.cpp中定义，并赋值，用lai_api_initialize初始化redis_lai对象，配置文件context_config.json

1. 在lai_redis_interfacequery.cpp中获取实际的redis_apis中的ocm api接口redis_ocm_api

1. redis_ocm_api定义在lai_redis_ocm.cpp

1. 最终的实现就是调用了redis_lai（实际定义在lai_redis_interfacequery.cpp）的set函数

1. 参数

1. objectType为LAI_OBJECT_TYPE_OCM

1. object_id为对应的ocm端口的oid，一个entry一个oid

1. attr为entry里的一个属性，这里为scan

1. 函数体

1. 最后调的是RedisRemoteLaiInterface.cpp中的set