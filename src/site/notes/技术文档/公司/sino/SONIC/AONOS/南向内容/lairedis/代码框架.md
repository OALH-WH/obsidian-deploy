---
{"dg-publish":true,"permalink":"/技术文档/公司/sino/SONIC/AONOS/南向内容/lairedis/代码框架/","dg-note-properties":{}}
---

1. 项目路径：sonic-lairedis

1. 入口函数

1. syncd_main.cpp 

1. 创建VendorLai对象（initialize时调用laimeta实现apis的构建）

1. 创建Syncd对象

1. 构造函数

1. 中会执行VendorLai的initialize函数

1. syncd_main -> Syncd.cpp : run()

1. Syncd.cpp: 构造函数

1. ...

1. 设置m_ln的各种处理函数

1. 设置线卡notifications处理函数根据attr.id设为m_ln中给定的处理函数

1. 设置告警上报处理函数NotificationHandler::onLinecardAlarm

1. Syncd.cpp : run

1. 添加rpc，flexcounter...到epoll中

1. 给对应槽道上的LINECARD表的每个key的field：oper-status、slot-status设置为ACTIVE和Ready

1. 使用了IO 多路复用技术

1. rpc请求：绑定的asic db（1）

1. flexCounter请求：绑定的flexcounter db（5）

1. processFlexCounterEvent

1. groupName为eg.1S_STAT_STATUS

1. strVid为eg.oid:0x2000000000027

1. vid(oid)为eg.0x2000000000027

1. vid转rid

1. 从map中拿

1. 如果不存在，则创建一个rid通过vid，然后存入map

1. 创建的时候从asic db中的VIDTORID的field[oid:...]中拿对应的value作为rid

1. sed去掉前缀oid:

1. 获取这个Key Field键值对向量到addCounter中

1. oper为set

1. 添加一个counter

1. 示例

```cpp
127.0.0.1:5000[5]> hgetall "FLEX_COUNTER_TABLE:1S_STAT_STATUS:oid:0x2000000000027"
1) "PORT_STATUS_ID_LIST"
2) "LAI_PORT_ATTR_LOW_THRESHOLD,LAI_PORT_ATTR_LED_MODE,LAI_PORT_ATTR_LOS_THRESHOLD,LAI_PORT_ATTR_LED_COLOR,LAI_PORT_ATTR_EQUIPMENT_MISMATCH,LAI_PORT_ATTR_EMPTY,LAI_PORT_ATTR_ADMIN_STATE,LAI_PORT_ATTR_OPER_STATUS,LAI_PORT_ATTR_REMOVABLE,LAI_PORT_ATTR_OPTICAL_PORT_TYPE,LAI_PORT_ATTR_HIGH_THRESHOLD,LAI_PORT_ATTR_EQUIPMENT_FAILURE"
127.0.0.1:5000[5]> hgetall "FLEX_COUNTER_TABLE:1S_STAT_GAUGE:oid:0x200000000000d"
1) "INPORT_GAUGE_ID_LIST"
2) "LAI_PORT_STAT_INPUT_POWER"
```

1. oper为remove

1. 删除一个counter

1. addCounter：FlexCounterManager.cpp

1. 参数

1. instanceId为group name

1. getInstance：FlexCounterManager.cpp

1. flexCounter的类型由group name决定

1. 创建FlexCounter类型数据，构造函数中会启动一个线程去collect数据

1. addCounter: FlexCounter.cpp

1. 根据vid获取object type： objectTypeQuery()

1. 创建数据collect器， 加入collectors中, index为vid来标识唯一object，例如aps，oa等

1. 把value值通过，分隔并存到set中，命名为counterIds

1. group

1. attr

1. state

1. gauge

1. rpc请求

1. ...