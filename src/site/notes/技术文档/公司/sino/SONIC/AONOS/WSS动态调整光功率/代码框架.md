---
{"dg-publish":true,"permalink":"/技术文档/公司/sino/SONIC/AONOS/WSS动态调整光功率/代码框架/","dg-note-properties":{}}
---

1. 代码路径

1. d4os/src/constant-power/src/constant-power

1. 框架解读

1. main.cpp

1. 创建对应1槽和5槽的WssOrch对象

1. 对象初始化

1. 创建table对象

1. 订阅以linecard为表名的所有键空间

1. 把linecard table指针放入选择器（IO多路复用）

1. 死循环

1. 判断键空间是否有变化

1. 有变化则遍历WssOrch对象执行对应的doTask

1. 判断指针是否为当前对象初始化时创建的table指针，不是则直接返回

1. 是的话执行以下处理

1. medial channel table

1. linecard table

1. 创建一个双端队列

1. 把有变化的key field value存入双端队列中

1. 处理linecard table状态变化

1. 只处理set操作

1. 继只处理set操作

1. 创建键值对的map

1. 判断键linecard-type的值是否为WSS32C

1. 把media channel table也加入到选择器中

1. 判断slot-status是否ready

1. ready

1. 启动锁定光功率的线程

1. not ready

1. 关闭线程

1. 清除所有告警

1. 不是WSS32C

1. 停止线程

1. 尝试从选择器中删除mediachannel table

1. 继启动锁定光功率的线程：constantPowerThreadRunFunction

1. 判断线程是否存在，且状态位为true

1. 说明有线程在运行，启动失败，直接返回

1. 启动一个线程里会创建一个线程，并状态位置为true

1. 遍历成员变量m_mediachannel_List //这个变量应该是在mediachannel table的订阅处理中去赋值

1. 没有targetPower直接continue

1. 获取实际的光功率，从counter db中的media channel_OutputPower:15_pm_current中的instant拿

1. 获取实际光衰，从state db中mediachannel 的attenuation-value中获取

1. 判断power-/+attenuation的绝对值是否大于1.5

1. 大于，更新init_power, continue

1. 更新init_power

1. 判断更新后的init光功率和光衰的差值

1. 小于等于0.3, reset pid, continue

1. bulk send

1. wait 指定秒：10s

1. 继medial channel table的处理

1. 如果是set操作

1. 遍历所有有变化的键值对

1. 查看是否有键：控制模式，

1. 查看模式是否为：动态光衰（即锁定光功率）

1. 查看mediachannel_List是否存在这个key

1. 如果source-port-name中不含子串COMIN，则continue到下一个键值对处理

1. 如果在mediachannel_List没找到这个key，则把这个key加入到mediachannel_List，并且在m_pid_map中添加对应key的PositionPID对象

1. 否则

1. 从m_mediachannel_list和m_pid_map中删除键值对

1. 如果键为：target-power

1. 查看这个key的目标光功率在m_targetPower中是否存在

1. 更新m_initPower,值也是从从counter db中的media channel_OutputPower:15_pm_current中拿的instant和attenuation-value值相加得到

1. 存在有更新和不存在的情况下会更新值到m_targetPower中

1. 如果是del操作

1. 从成员变量中删除这些值