---
{"dg-publish":true,"permalink":"/技术文档/公司/sino/SONIC/AONOS/南向内容/lairedis/代码框架1/","dg-note-properties":{}}
---

1. AttrCollector

1. 构建函数

1. 构建父类Collector

1. 获取state TableKeyName

1. 从counter db中拿key，根据object type

1. 遍历strAttrIds(即counterIds)

1. 通过strAttrlId获取属性对象meta

1. 通过meta创建一个attr

1. 通过VendorLai接口调用get函数

1. 通过lai_metadata_get_object_type_info获取指定info，然后调用info的get函数

1. 示例

1. 比如调port的get，最终会调Lai接口中的get_port_attribute：laiport.c

1. 参数

1. attr_list: &attr

1. attr_counter: 1

1. object type

1. 作用

1. 通过不同的属性id走不同的逻辑，并把值存入attr_list中

1. 把meta存入m_entries

1. 根据所有的meta创建对应的attr

1. collect

1. 遍历entries

1. 调用对应属性的get操作

1. 更新当前数据调updateCurrentValue

1. gaugeCollector

1. 构建函数

1. 